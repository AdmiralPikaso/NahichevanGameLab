<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="mb-0">
      <i class="bi bi-heart text-danger"></i> 
      Запись вишлиста
    </h1>
    <%= link_to wishlists_path, class: "btn btn-outline-secondary" do %>
      <i class="bi bi-arrow-left"></i> Назад к вишлистам
    <% end %>
  </div>

  <% if @wishlist %>
    <div class="card">
      <div class="card-header">
        <h5 class="mb-0"><%= @wishlist.game.title %></h5>
      </div>
      <div class="card-body">
        <p>Эта страница показывает отдельную запись вишлиста.</p>
        <p>Приоритет: <span class="badge bg-<%= @wishlist.priority_color %>">
          <%= @wishlist.priority_icon %> <%= @wishlist.priority_name %>
        </span></p>
        
        <div class="mt-3">
          <%= link_to "На страницу игры", game_path(@wishlist.game), class: "btn btn-primary" %>
          <%= link_to "Вернуться к списку вишлистов", wishlists_path, class: "btn btn-secondary" %>
        </div>
      </div>
    </div>
  <% else %>
    <div class="alert alert-warning">
      <p class="mb-0">Запись вишлиста не найдена.</p>
    </div>
  <% end %>
</div>