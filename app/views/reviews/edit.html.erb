<div class="container">
  <h1>Редактирование рецензии на игру: <%= @review.game.title %></h1>
  
  <%= render 'form', game: @review.game, review: @review %>
  
  <div class="mt-3">
    <%= link_to "← Назад к рецензии", review_path(@review), class: "btn btn-outline-secondary" %>
  </div>
</div>