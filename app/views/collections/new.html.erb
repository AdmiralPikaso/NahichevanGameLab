<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card">
        <div class="card-header">
          <h2 class="mb-0">Создание новой коллекции</h2>
        </div>
        
        <div class="card-body">
          <%= form_with(model: @collection, url: collections_path, local: true) do |form| %>
            <% if @collection.errors.any? %>
              <div class="alert alert-danger">
                <h5><%= pluralize(@collection.errors.count, "ошибка") %> предотвратила сохранение:</h5>
                <ul>
                  <% @collection.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            <% end %>
            
            <div class="mb-3">
              <%= form.label :name, "Название коллекции", class: "form-label" %>
              <%= form.text_field :name, class: "form-control", placeholder: "Например: 'Лучшие RPG'" %>
              <div class="form-text">
                Придумайте понятное название для вашей коллекции (2-100 символов)
              </div>
            </div>
            
            <div class="mb-4">
              <%= form.label :description, "Описание", class: "form-label" %>
              <%= form.text_area :description, class: "form-control", rows: 4, 
                    placeholder: "Опишите, какие игры будут в этой коллекции..." %>
              <div class="form-text">
                Необязательное поле (до 500 символов)
              </div>
            </div>
            
            <div class="mb-4">
              <h5>Идеи для коллекций:</h5>
              <div class="row">
                <div class="col-md-6">
                  <ul class="text-muted">
                    <li>По жанрам (Шутеры, RPG, Стратегии)</li>
                    <li>По платформам (PC, PlayStation, Nintendo)</li>
                    <li>По годам (Игры 2020-х, Классика)</li>
                  </ul>
                </div>
                <div class="col-md-6">
                  <ul class="text-muted">
                    <li>По настроению (На расслабление, Для сложных задач)</li>
                    <li>Для мультиплеера</li>
                    <li>С лучшей графикой</li>
                  </ul>
                </div>
              </div>
            </div>
            
            <div class="d-flex justify-content-between">
              <%= link_to "← Отмена", collections_path, class: "btn btn-secondary" %>
              <%= form.submit "Создать коллекцию", class: "btn btn-primary" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>